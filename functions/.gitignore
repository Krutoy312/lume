# Compiled JavaScript files
lib/**/*.js
lib/**/*.js.map

# TypeScript v1 declaration files
typings/

# Node.js dependency directory
node_modules/

# Environment variable files
# .env is committed (non-secret defaults only)
# .env.local and project-specific overrides must NOT be committed
.env.local
.env.*.local
*.local