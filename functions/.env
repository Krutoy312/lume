# Non-secret runtime configuration for Cloud Functions.
# Safe to commit — no credentials here.
# For local overrides create .env.local (git-ignored).
# For secrets use: firebase functions:secrets:set SECRET_NAME

REGION=europe-west1
